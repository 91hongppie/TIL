```python
190723 homework.md
1. 
from fibo import fibo_recursion as fr
2.
ZeroDivisionError 0으로 나눌 때
NameError 정의되지 않은 변수를 호출 했을 때
TypeError 1 + '1' 처럼 타입이 잘못되었을 때, 자료형에 대한 잘못된 사용
IndexError 리스트에서 없는 인덱스를 불러올 때, index 범위를 벗어나서 참조하려 할 때
KeyError dictionary 에 없는 key 값을 호출할 때
ModuleNotFoundError Module 을 찾을 수 없을 때 ex) import ssafy
ImportError 모듈은 찾았으나 가져오는 과정에서 실패하는 경우(대부분 없는 클래스 / 메서드 이름을 불러옴)
```

