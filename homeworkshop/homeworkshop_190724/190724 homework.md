```python
190724 homeworkshop
1.
int
str
dict
list
map
zip
complex
float
2.
__init__ : 인스턴스 객체가 생성될 때 호출되는 함수
__del__ : 인스턴스 소멸되기 직전에 호출되는 함수
__str__ : 인스턴스를 print() 할 때 보여주는 값을 반환(반환 값은 반드시 문자열)
__repr__ : 인스턴스 자체가 반환할 값을 새로운 문자열 표현으로 반환(반환 값은 반드시 문자열)
3.
list - append(), count(), sort()

str - upper(), lower()

dict - get(), update(), items()
```

